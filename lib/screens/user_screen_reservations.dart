import 'package:flutter/material.dart';

class UserScreenReservations extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('réservations du passager'),
        );
  }
}